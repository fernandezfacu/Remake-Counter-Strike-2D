#ifndef WEAPON_H
#define WEAPON_H

#include <cstdint>
#include <string>
#include <unordered_map>
#include <utility>

enum Weapon { NONE, GLOCK, AK47, M3, AWP, UNKNOWN_WEAPON };

enum WeaponType { PRIMARY, SECONDARY, UNKNOWN_WEAPON_TYPE };

#endif
